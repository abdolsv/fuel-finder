PORT=5000

DATABASE_URL=postgres://fueluser:fuelpass@localhost:5432/fuelfinder
DB_NAME=fuelfinder
DB_USER=fueluser
DB_PASS=fuelpass
DB_HOST=localhost
DB_PORT=5432

# --- Auth ---
# Any long random string. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=12h

# --- Admin seed ---
ADMIN_EMAIL=
ADMIN_PASSWORD=
